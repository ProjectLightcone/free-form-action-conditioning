dataset_version: puck-v0.1-counterfactual-eval-json
seed: 29
output_dir: data/processed/puck-v0.1-counterfactual-eval-json
run_dir: reports/runs
checkpoint_dir: checkpoints
generation_mode: counterfactual_eval
counterfactual_split: counterfactual
image_size: 64
horizon: 40
num_scenes: 256
events_per_scene: 64
renderings_per_event: 4
min_pucks: 2
max_pucks: 2
friction: 0.985
restitution: 0.86
scene_val_fraction: 0.0
heldout_template_fraction: 0.0
batch_size: 256
epochs: 1
learning_rate: 0.001
optimizer: adamw
weight_decay: 0.001
lr_schedule: cosine
min_learning_rate: 0.00005
lr_decay_start_epoch: 20
early_stopping_enabled: true
early_stopping_monitor: val_target_latent_mse
early_stopping_min_delta: 0.0005
early_stopping_patience: 25
early_stopping_min_epochs: 45
latent_dim: 128
max_text_len: 192
heatmap_size: 32
normalize_latents: false
sigreg_weight: 0.1
sigreg_num_slices: 1024
sigreg_num_points: 17
sigreg_integration_bound: 5.0
sigreg_on_predictions: false
prediction_loss_weight: 0.25
latent_loss_weight: 5.0
target_reconstruction_loss_weight: 0.0
context_state_loss_weight: 5.0
target_sigreg_weight: 0.02
target_sigreg_warmup_epochs: 10
target_sigreg_ramp_epochs: 20
condition_family_filter:
  - json
condition_families:
  json: 1.0
